import { LLMController } from '../../controllers/LLMController';

// I don't know what else can I include here? It seems to introduce more confusion and unneccesary layer. Might as well move Controller into this file?
export const LLMModule = LLMController;
