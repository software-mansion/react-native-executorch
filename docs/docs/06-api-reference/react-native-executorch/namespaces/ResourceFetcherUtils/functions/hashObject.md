# Function: hashObject()

> **hashObject**(`jsonString`): `string`

Defined in: [utils/ResourceFetcherUtils.ts:131](https://github.com/software-mansion/react-native-executorch/blob/main/packages/react-native-executorch/src/utils/ResourceFetcherUtils.ts#L131)

Generates a hash from a string representation of an object.

## Parameters

### jsonString

`string`

The stringified JSON object to hash.

## Returns

`string`

The resulting hash as a string.
