# Type Alias: LLMTool

> **LLMTool** = `Object`

Defined in: [types/llm.ts:311](https://github.com/software-mansion/react-native-executorch/blob/0e95b8934cc7318c1b30a8e534444a8b50d25230/packages/react-native-executorch/src/types/llm.ts#L311)

Represents a tool that can be used by the model.
Usually tool is represented with dictionary (Object), but fields depend on the model.
Unfortunately there's no one standard so it's hard to type it better.
