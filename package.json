{
  "name": "react-native-executorch-monorepo",
  "version": "0.0.0",
  "packageManager": "yarn@4.1.1",
  "// === LEGACY SUPPORT: workspaces ===": "apps/legacy/* contains preserved legacy example apps (remove when legacy apps are dropped)",
  "workspaces": {
    "packages": [
      "packages/*",
      "apps/*",
      "apps/legacy/*"
    ]
  },
  "scripts": {
    "prepare": "yarn workspaces foreach --all --exclude react-native-executorch-webrtc --topological-dev run prepare",
    "lint": "yarn workspaces foreach --all --exclude react-native-executorch-webrtc --parallel run lint",
    "typecheck": "yarn workspaces foreach --all --exclude react-native-executorch-webrtc --parallel run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@babel/core": "^7.29.7",
    "@babel/eslint-parser": "^7.25.1",
    "@babel/preset-react": "^7.29.0",
    "@cspell/eslint-plugin": "^8.19.0",
    "@evilmartians/lefthook": "^2.1.1",
    "@react-native/eslint-config": "^0.79.0",
    "cspell": "^8.19.0",
    "eslint": "^8.57.0",
    "eslint-plugin-ft-flow": "^2.0.3",
    "eslint-plugin-jsdoc": "^62.7.1",
    "eslint-plugin-markdown": "^5.1.0",
    "eslint-plugin-prettier": "^5.0.1",
    "expo-router": "~6.0.17",
    "prettier": "^3.3.3",
    "prettier-plugin-jsdoc": "^1.3.0",
    "react-native-worklets": "0.10.3",
    "typescript": "~5.9.2"
  },
  "resolutions": {
    "react-native-worklets": "0.10.3"
  }
}
