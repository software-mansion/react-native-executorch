name: CI
on:
  push:
    branches:
      - main
      - rne-rewrite
  pull_request:
    branches:
      - main
      - rne-rewrite
  merge_group:
    types:
      - checks_requested
  workflow_dispatch:
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup
        uses: ./.github/actions/setup

      - name: Lint files
        run: yarn lint

      - name: Typecheck files
        run: yarn workspaces foreach --all --exclude react-native-executorch-webrtc --topological-dev run prepare && yarn typecheck

      - name: Verify error codes are up to date
        run: |
          yarn codegen:errors
          git diff --exit-code -- \
            packages/react-native-executorch/src/errors/codes.ts \
            packages/react-native-executorch/cpp/core/error_codes.h \
            || { echo "::error::Error codes are stale. Run 'yarn codegen:errors' and commit the result."; exit 1; }

  build-library:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup
        uses: ./.github/actions/setup

      - name: Build all packages
        run: yarn workspaces foreach --all --exclude react-native-executorch-webrtc --topological-dev run prepare
